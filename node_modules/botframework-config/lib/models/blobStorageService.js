"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Copyright(c) Microsoft Corporation.All rights reserved.
 * Licensed under the MIT License.
 */
const schema_1 = require("../schema");
const azureService_1 = require("./azureService");
class BlobStorageService extends azureService_1.AzureService {
    constructor(source = {}) {
        super(source, schema_1.ServiceTypes.BlobStorage);
    }
    // encrypt keys in service
    encrypt(secret, encryptString) {
        if (this.connectionString && this.connectionString.length > 0) {
            this.connectionString = encryptString(this.connectionString, secret);
        }
    }
    // decrypt keys in service
    decrypt(secret, decryptString) {
        if (this.connectionString && this.connectionString.length > 0) {
            this.connectionString = decryptString(this.connectionString, secret);
        }
    }
}
exports.BlobStorageService = BlobStorageService;
//# sourceMappingURL=blobStorageService.js.map